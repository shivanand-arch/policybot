run = "pip install -r requirements.txt && python app.py"
entrypoint = "app.py"
modules = ["python-3.11"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "gunicorn --bind :8080 --workers 2 --threads 4 --timeout 120 app:app"]

[[ports]]
localPort = 8080
externalPort = 80
